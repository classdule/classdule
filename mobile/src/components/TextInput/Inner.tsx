import { View } from "react-native";
import { styled } from "../../styles/stitches";

export const TextInputInner = styled(View, {
  paddingHorizontal: 8,
  flexDirection: "row",
  alignItems: "center",
  flex: 1,
});
